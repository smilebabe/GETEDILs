# Core dependencies - Optimized for 512MB RAM
fastapi==0.104.1
uvicorn[standard]==0.24.0
asyncpg==0.29.0
httpx==0.25.1
pydantic==2.4.2

# Optional (uncomment if needed)
# redis==5.0.1  # Only if you have Redis
# python-dotenv==1.0.0
